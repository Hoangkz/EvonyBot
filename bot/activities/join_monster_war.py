"""
join_monster_war.py — "Join Monster War" activity.
"""


def run(bot, settings: dict):
    """`bot` is the device's BotContext; `settings` is the "Join Monster War" tab's config."""
    # TODO: real automation.
    bot.log("Join Monster War")
    bot.sleep(1.0)
