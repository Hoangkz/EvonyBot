"""
join_monster_war — "Join Monster War" activity (port of C# Boss).

- run.py:       entry point, `run(bot, settings)`, and the _Boss loop
- constants.py: images, limits and action names
"""
from .run import run

__all__ = ["run"]
