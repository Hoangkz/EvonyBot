"""
battlefield_shop.py — "Battlefield Shop" activity.
"""


def run(bot, settings: dict):
    """`bot` is the device's BotContext; `settings` is the "Battlefield Shop" tab's config."""
    # TODO: real automation.
    bot.log("Battlefield Shop")
    bot.sleep(1.0)
