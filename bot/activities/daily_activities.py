"""
daily_activities.py — "Daily Activities" activity.
"""


def run(bot, settings: dict):
    """`bot` is the device's BotContext; `settings` is the "Daily Activities" tab's config."""
    # TODO: real automation.
    bot.log("Daily Activities")
    bot.sleep(1.0)
