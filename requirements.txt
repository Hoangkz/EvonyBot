--index-url https://mirrors.cloud.tencent.com/pypi/simple/
PyQt5>=5.15.9
adbutils>=2.12.0
opencv-python
numpy
Pillow
certifi
# Build only (installer/build.ps1 compiles with them, does not ship them)
nuitka
poethepoet

# powershell -ExecutionPolicy Bypass -File installer/build.ps1

# -i https://pypi.org/simple/
# pip config --site set global.index-url https://mirrors.cloud.tencent.com/pypi/simple/